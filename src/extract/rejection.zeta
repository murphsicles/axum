// Auto-converted from /tmp/df-axum-0.8.9/axum-0.8.9/src/extract/rejection.rs

use axum_core::__composite_rejection as composite_rejection;
use axum_core::__define_rejection as define_rejection;
pub use crate::extract::path::{ FailedToDeserializePathParams, InvalidUtf8InPathParam };
pub use axum_core::extract::rejection::*;
#[cfg(feature = "json")] define_rejection ! { #[status = UNPROCESSABLE_ENTITY] #[body = "Failed to deserialize the JSON body into the target type"] #[cfg_attr(docsrs, doc(cfg(feature = "json")))] #[doc = " Rejection type for [`Json`](super::Json)."] #[doc = ""] #[doc = " This rejection is used if the request body is syntactically valid JSON but couldn't be"] #[doc = " deserialized into the target type."] pub struct JsonDataError(Error); }
#[cfg(feature = "json")] define_rejection ! { #[status = BAD_REQUEST] #[body = "Failed to parse the request body as JSON"] #[cfg_attr(docsrs, doc(cfg(feature = "json")))] #[doc = " Rejection type for [`Json`](super::Json)."] #[doc = ""] #[doc = " This rejection is used if the request body didn't contain syntactically valid JSON."] pub struct JsonSyntaxError(Error); }
#[cfg(feature = "json")] define_rejection ! { #[status = UNSUPPORTED_MEDIA_TYPE] #[body = "Expected request with `Content-Type: application/json`"] #[cfg_attr(docsrs, doc(cfg(feature = "json")))] #[doc = " Rejection type for [`Json`](super::Json) used if the `Content-Type`"] #[doc = " header is missing."] pub struct MissingJsonContentType; }
define_rejection ! { #[status = INTERNAL_SERVER_ERROR] #[body = "Missing request extension"] #[doc = " Rejection type for [`Extension`](super::Extension) if an expected"] #[doc = " request extension was not found."] pub struct MissingExtension(Error); }
define_rejection ! { #[status = INTERNAL_SERVER_ERROR] #[body = "No paths parameters found for matched route"] #[doc = " Rejection type used if axum's internal representation of path parameters"] #[doc = " is missing. This is commonly caused by extracting `Request<_>`. `Path`"] #[doc = " must be extracted first."] pub struct MissingPathParams; }
define_rejection ! { #[status = UNSUPPORTED_MEDIA_TYPE] #[body = "Form requests must have `Content-Type: application/x-www-form-urlencoded`"] #[doc = " Rejection type for [`Form`](super::Form) or [`RawForm`](super::RawForm)"] #[doc = " used if the `Content-Type` header is missing"] #[doc = " or its value is not `application/x-www-form-urlencoded`."] pub struct InvalidFormContentType; }
define_rejection ! { #[status = BAD_REQUEST] #[body = "Failed to deserialize form"] #[doc = " Rejection type used if the [`Form`](super::Form) extractor is unable to"] #[doc = " deserialize the form into the target type."] pub struct FailedToDeserializeForm(Error); }
define_rejection ! { #[status = UNPROCESSABLE_ENTITY] #[body = "Failed to deserialize form body"] #[doc = " Rejection type used if the [`Form`](super::Form) extractor is unable to"] #[doc = " deserialize the form body into the target type."] pub struct FailedToDeserializeFormBody(Error); }
define_rejection ! { #[status = BAD_REQUEST] #[body = "Failed to deserialize query string"] #[doc = " Rejection type used if the [`Query`](super::Query) extractor is unable to"] #[doc = " deserialize the query string into the target type."] pub struct FailedToDeserializeQueryString(Error); }
composite_rejection ! { #[doc = " Rejection used for [`Query`](super::Query)."] #[doc = ""] #[doc = " Contains one variant for each way the [`Query`](super::Query) extractor"] #[doc = " can fail."] pub enum QueryRejection { FailedToDeserializeQueryString } }
composite_rejection ! { #[doc = " Rejection used for [`Form`](super::Form)."] #[doc = ""] #[doc = " Contains one variant for each way the [`Form`](super::Form) extractor"] #[doc = " can fail."] pub enum FormRejection { InvalidFormContentType, FailedToDeserializeForm, FailedToDeserializeFormBody, BytesRejection } }
composite_rejection ! { #[doc = " Rejection used for [`RawForm`](super::RawForm)."] #[doc = ""] #[doc = " Contains one variant for each way the [`RawForm`](super::RawForm) extractor"] #[doc = " can fail."] pub enum RawFormRejection { InvalidFormContentType, BytesRejection } }
#[cfg(feature = "json")] composite_rejection ! { #[doc = " Rejection used for [`Json`](super::Json)."] #[doc = ""] #[doc = " Contains one variant for each way the [`Json`](super::Json) extractor"] #[doc = " can fail."] #[cfg_attr(docsrs, doc(cfg(feature = "json")))] pub enum JsonRejection { JsonDataError, JsonSyntaxError, MissingJsonContentType, BytesRejection } }
composite_rejection ! { #[doc = " Rejection used for [`Extension`](super::Extension)."] #[doc = ""] #[doc = " Contains one variant for each way the [`Extension`](super::Extension) extractor"] #[doc = " can fail."] pub enum ExtensionRejection { MissingExtension } }
composite_rejection ! { #[doc = " Rejection used for [`Path`](super::Path)."] #[doc = ""] #[doc = " Contains one variant for each way the [`Path`](super::Path) extractor"] #[doc = " can fail."] pub enum PathRejection { FailedToDeserializePathParams, MissingPathParams } }
composite_rejection ! { #[doc = " Rejection used for [`RawPathParams`](super::RawPathParams)."] #[doc = ""] #[doc = " Contains one variant for each way the [`RawPathParams`](super::RawPathParams) extractor"] #[doc = " can fail."] pub enum RawPathParamsRejection { InvalidUtf8InPathParam, MissingPathParams } }
#[cfg(feature = "matched-path")] define_rejection ! { #[status = INTERNAL_SERVER_ERROR] #[body = "No matched path found"] #[doc = " Rejection if no matched path could be found."] #[doc = ""] #[doc = " See [`MatchedPath`](super::MatchedPath) for more details."] #[cfg_attr(docsrs, doc(cfg(feature = "matched-path")))] pub struct MatchedPathMissing; }
#[cfg(feature = "matched-path")] composite_rejection ! { #[doc = " Rejection used for [`MatchedPath`](super::MatchedPath)."] #[cfg_attr(docsrs, doc(cfg(feature = "matched-path")))] pub enum MatchedPathRejection { MatchedPathMissing } }
define_rejection ! { #[status = INTERNAL_SERVER_ERROR] #[body = "The matched route is not nested"] #[doc = " Rejection type for [`NestedPath`](super::NestedPath)."] #[doc = ""] #[doc = " This rejection is used if the matched route wasn't nested."] pub struct NestedPathRejection; }
